#ifndef algorithms_h
#define algorithms_h

#include <iostream>
#include <algorithm>


void Interchange_sort(int* arr, int size);								// interchange sort algorithm

void Bubble_sort(int* arr, int size);									// bubble sort algorithm

void Selection_sort(int* arr, int size);								// selection sort algorithm

void Heap_sort(int* arr, int size);										// heap sort algorithm

void Merge_sort(int* arr, int size);									// merge sort algorithm

void Quick_sort(int* arr, int size);									// quick sort algorithm: pivot between array

void Quick_sort_randomized(int* arr, int size);							// quick sort algorithm: pivot random

void Radix_sort(int* arr, int size);									// radix sort algorithm

#endif !algorithms_h
